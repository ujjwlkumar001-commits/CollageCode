#include<stdio.h>
int main ()
{
  int a,b,c;
  printf("Enter A:");
  scanf("%d", &a);
  printf("Enter B:");
  scanf("%d", &b);
  printf("Enter C:");
  scanf("%d", &c);
  a<b? (a<c? printf("A is lowest"): printf("C is lowest")):(b<c? printf("B is lowest"): printf("C is lowest"));
  return 0;
}
